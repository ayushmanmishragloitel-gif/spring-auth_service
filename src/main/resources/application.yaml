spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://144.24.120.211:5432/auth_service
    username: postgres
    password: fgjfrjkdm3473943
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public
  flyway:
    enabled: true
    locations: classpath:db/migration
    default-schema: public
    schemas: public
